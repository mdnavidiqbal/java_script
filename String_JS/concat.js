const first = 'Navid';
const second = 'Iqbal';
const full_name = first+ ' ' +second;
// console.log(full_name);

console.log(first.concat(' ').concat(second));

// include() function we use it to find or search the value 

console.log(second.includes('l'));
