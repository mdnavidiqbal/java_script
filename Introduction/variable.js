/* 
                           ********************Variable**********************

* Variable : Variable means container or storage where we store and contain data alos we change the value
* Five step to declare a variable : 1. var (declare variable) 2. variable_name 3. = (Equal-Sign) 4.value(data) 5.;(sem-col)
* We can't use any keyword in variable name 
* No space or gap in the variable name
*/
// Declare a variable //
var Name = "Navid"
console.log(Name);

// Change value
Name = "Iqbal";
console.log(Name)

// Undefined Variable //
var age;
console.log(age) 
// Output: Undefined 
// Constant variabel: when we assign a value on this variable we can't change this further cz its immuteable

const color = "green";
console.log(color)
// color = "red";
// Output : code error